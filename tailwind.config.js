/** @type {import('tailwindcss').Config} */
export const content = [
  './index.html', // If you have an index.html in the root (e.g., in public/)
  './src/**/*.{vue,js,ts,jsx,tsx}', // Scans all Vue, JS, TS files in src/
];
export const theme = {
  extend: {},
};
export const plugins = [];
